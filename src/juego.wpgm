import wollok.game.*
import escenario.*
import background.*
import auto.*


program juego {

	game.title("Juego Base")
	game.cellSize(10)
	game.height(84)
	game.width(80)
	background.crearFondo()
	
	game.start()
}