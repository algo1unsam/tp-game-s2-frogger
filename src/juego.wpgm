import wollok.game.*
import escenario.*
import background.*
import objetosMoviles.*
import niveles.*
import rana.*

program juego {
	game.title("Juego Base")
	game.cellSize(10)
	game.height(background.limite_y() + (background.tamanio_celda() / 2))
	game.width(background.limite_x())
	
	new Nivel1().iniciar()
	
	game.start()
	
}