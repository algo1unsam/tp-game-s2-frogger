import wollok.game.*
import pepita.*
import escenario.*
import background.*


program juego {
	game.title("Juego Base")
	game.addVisual(pepita)
	game.cellSize(10)
	game.height(84)
	game.width(80)
	background.crearFondo()
	
	game.start()
}